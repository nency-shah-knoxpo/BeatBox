<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/red</item>//toolbar color
        <item name="colorPrimaryDark">@color/dark_red</item>//status bar color
        <item name="colorAccent">@color/gray</item>//tint
        <item name="android:colorBackground">@color/soothing_blue</item>
    </style>

    <style name="BeatBoxButton">
        <item name="android:background">@drawable/button_beat_box</item>
    </style>
    <style name="BeatBoxButton.Strong">
        <item name="android:textStyle">bold</item>
    </style>

</resources>
